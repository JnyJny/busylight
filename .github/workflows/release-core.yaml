name: "Release: busylight-core"

on:
  push:
    tags:
      - 'busylight-core/v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write
  id-token: write
  pull-requests: read

jobs:
  get-python-versions:
    name: Get Python test versions
    runs-on: ubuntu-latest
    outputs:
      python-versions: ${{ steps.extract.outputs.versions }}
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@v7
      - name: Extract Python test versions
        id: extract
        run: |
          if versions=$(uv run toml get --toml-path packages/busylight-core/pyproject.toml tool.busylight_core.ci.test-python-versions 2>/dev/null); then
            echo "versions=$versions" >> $GITHUB_OUTPUT
          else
            echo 'versions=["3.11", "3.12", "3.13"]' >> $GITHUB_OUTPUT
          fi

  test:
    name: "Test (${{ matrix.python-version }}, ${{ matrix.os }})"
    needs: get-python-versions
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ${{ fromJSON(needs.get-python-versions.outputs.python-versions) }}
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libudev-dev libusb-1.0-0-dev

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true

      - name: Run unit tests
        run: uv run --all-extras pytest packages/busylight-core/tests/

      - name: Run doc tests
        run: uv run --all-extras pytest --markdown-docs packages/busylight-core/docs/ --ignore=packages/busylight-core/docs/gen_ref_pages.py

  build:
    name: Build Package
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Build package
        run: uv build --package busylight_core

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: core-dist
          path: dist/
          retention-days: 1

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi-core
      url: https://pypi.org/project/busylight_core
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: core-dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  github-release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: core-dist
          path: dist/

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install dependencies
        run: uv sync

      - name: Generate release notes
        run: |
          uv run git cliff --include-path 'packages/busylight-core/**' \
            --latest --strip header > release_notes.md

      - name: Update CHANGELOG
        run: |
          uv run git cliff --include-path 'packages/busylight-core/**' \
            --output packages/busylight-core/CHANGELOG.md

      - name: Commit updated CHANGELOG
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add packages/busylight-core/CHANGELOG.md
          git diff --cached --quiet || git commit -m "docs(core): update CHANGELOG"
          git push origin HEAD:main

      - name: Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "dist/*"
          bodyFile: "release_notes.md"
          name: "busylight-core ${{ github.ref_name }}"
          tag: ${{ github.ref_name }}
          draft: false
          prerelease: false
          generateReleaseNotes: false
          token: ${{ secrets.GITHUB_TOKEN }}

  deploy-docs:
    name: Deploy Core Documentation
    needs: [publish, github-release]
    runs-on: ubuntu-latest
    steps:
      - name: Trigger docs deployment
        uses: peter-evans/repository-dispatch@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          event-type: release-core
          client-payload: '{"ref": "${{ github.ref }}", "sha": "${{ github.sha }}"}'

  check-cli-compat:
    name: Check CLI compatibility
    needs: publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libudev-dev libusb-1.0-0-dev

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Run CLI tests against new core
        run: uv run --all-extras pytest packages/busylight/tests/

      - name: Report result
        if: always()
        run: |
          if [ "${{ job.status }}" = "success" ]; then
            echo "CLI tests pass against new core release."
          else
            echo "::warning::CLI tests fail against new core release. A CLI update may be needed."
          fi
